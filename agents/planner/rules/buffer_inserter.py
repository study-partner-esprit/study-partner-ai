"""Buffer inserter for adding buffer time to tasks."""


def insert_buffers(tasks):
    """Add buffer time between tasks for breaks and transitions."""
    # Simple implementation - just return tasks as-is for now
    # In a real implementation, this would add buffer tasks or adjust timing
    return tasks
