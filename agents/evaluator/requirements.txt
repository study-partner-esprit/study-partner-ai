# Evaluator agent — LLM/DB/STT dependencies live in the repo root pyproject.toml.
# The evaluator routes through the shared LiteLLM client (utils/llm_client.py);
# session state is in-memory. This file only pins the direct test/validation
# dependencies for the evaluator package itself.
pydantic>=2.0
pytest>=7.0
